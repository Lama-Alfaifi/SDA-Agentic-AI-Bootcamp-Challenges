version: "3.8"

services:
  ntp-rag-app:
    build: .
    container_name: ntp_rag_assistant
    ports:
      - "8000:8000"
      - "8501:8501"
    env_file:
      - .env
    environment:
      - API_HOST=0.0.0.0
      - API_PORT=8000
      - STREAMLIT_PORT=8501
    restart: unless-stopped
